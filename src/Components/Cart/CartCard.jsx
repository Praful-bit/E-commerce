/* eslint-disable react/prop-types */

import { useState } from "react";
import { userProduct } from "../../Context/ProductContext";

function CartCard({ products }) {
 
  const { deleteCartItem,setQuantity } = userProduct();
 
  

  return (
    <>
     
    
    </>
  );
}

export default CartCard;
